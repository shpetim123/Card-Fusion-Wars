using UnityEngine;
using System.Collections;

public class PlaceCard : MonoBehaviour {

	// Use this for initialization
	void Start ()
	{
	
	}
	
	// Update is called once per frame
	void Update ()
	{
	
	}

	/*
	 * Get a reference to the card being placed
	 * Get a reference to the tile being pressed
	 * 
	 * Assign the "occupant" variable in the tile to the card being placed
	 * */
}
